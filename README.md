In "CRUD".postman •  Designed and executed REST API tests using Postman for a user management environment (Reqres).
•  Tested core CRUD operations (GET, POST, PUT, DELETE) to verify data integrity and server responses.
•  Implemented basic automated assertions in Postman to validate HTTP status codes (200, 201, 204).
------------------------------------------------------------------------------------------------------------------------------
In"Hubspot_CRM"  project I connected Postman to HubSpot CRM using Bearer Token, and created automated tests for GET and POST requests...
------------------------------------------------------------------------------------------------------------------------------
